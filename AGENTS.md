# AGENTS.md

## 작업 원칙
- 작업은 항상 **작은 단위**로 진행한다. (한 PR에 한 기능/한 모듈)
- 변경 후 반드시 **로컬 실행 방법**과 **테스트 방법(요청 예시)**을 README 또는 PR 설명에 포함한다.
- 기존 API 경로는 가능하면 유지한다.
- 임의로 대규모 리팩터링을 하지 않는다. (필요 시 제안만 한다)
- 에러가 발생하면 에러 로그, 원인, 수정안을 함께 제출한다.

## 현재 목표 (1차 MVP)
ERP + AI의 1차 MVP 목표는 아래 흐름을 구현하는 것이다.
1. 자동 발주 추천 초안 생성
2. 관리자 승인
3. 발주 확정
