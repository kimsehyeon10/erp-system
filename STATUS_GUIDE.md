# 📊 재고 상태(Status) 메뉴얼

## 5️⃣ 요구사항: 재고 상태 정의 및 기준

---

## 🎯 재고 상태 종류

ERP 재고 관리 시스템에서는 다음 5가지 상태를 사용합니다:

| 상태 | 표시 색상 | 의미 | 조건 |
|-----|---------|------|------|
| **품절** | 빨강 (badge-danger) | 재고가 완전히 소진됨 | `현재 재고 = 0` |
| **긴급** | 빨강 (badge-danger) | 즉시 발주 필요 | `현재 재고 < 안전재고 × 0.5` |
| **부족** | 노랑 (badge-warning) | 재고 보충 필요 | `안전재고 × 0.5 ≤ 현재 재고 < 안전재고` |
| **정상** | 초록 (badge-success) | 적정 재고 유지 | `안전재고 ≤ 현재 재고 ≤ 안전재고 × 3` |
| **과다** | 파랑 (badge-info) | 재고 과잉 | `현재 재고 > 안전재고 × 3` |

---

## 📐 상태 판정 기준 상세

### 1. 품절 (Critical - Out of Stock)
```
조건: 현재 재고 = 0
색상: 빨강 (badge-danger)
```

**의미:**
- 해당 상품의 재고가 완전히 소진된 상태
- 즉시 입고 또는 발주가 필요
- 주문 대응 불가

**대응 방법:**
1. 긴급 발주 요청
2. 고객 주문 대기 안내
3. 대체 상품 제안

---

### 2. 긴급 (Critical - Urgent)
```
조건: 0 < 현재 재고 < 안전재고 × 0.5
색상: 빨강 (badge-danger)
```

**의미:**
- 안전재고의 절반 미만으로 재고가 급감
- 품절이 임박한 위험 상태
- 긴급 발주 필요

**예시:**
- 안전재고: 20개
- 현재 재고: 8개
- 판정: **긴급** (8 < 20 × 0.5 = 10)

**대응 방법:**
1. 즉시 발주 처리
2. 입고 예정일 확인
3. 재고 소진 속도 모니터링

---

### 3. 부족 (Low Stock)
```
조건: 안전재고 × 0.5 ≤ 현재 재고 < 안전재고
색상: 노랑 (badge-warning)
```

**의미:**
- 안전재고 미달 상태
- 조만간 발주 필요
- 정상 운영은 가능하지만 주의 필요

**예시:**
- 안전재고: 20개
- 현재 재고: 15개
- 판정: **부족** (10 ≤ 15 < 20)

**대응 방법:**
1. 발주 계획 수립
2. 소진 예상 시점 계산
3. 발주량 결정

---

### 4. 정상 (Normal)
```
조건: 안전재고 ≤ 현재 재고 ≤ 안전재고 × 3
색상: 초록 (badge-success)
```

**의미:**
- 적정 재고 수준 유지
- 정상적인 운영 가능
- 특별한 조치 불필요

**예시:**
- 안전재고: 20개
- 현재 재고: 45개
- 판정: **정상** (20 ≤ 45 ≤ 60)

**대응 방법:**
- 현재 상태 유지
- 정기적인 재고 모니터링
- 소진 추이 관찰

---

### 5. 과다 (Excess)
```
조건: 현재 재고 > 안전재고 × 3
색상: 파랑 (badge-info)
```

**의미:**
- 안전재고의 3배를 초과하는 과잉 재고
- 창고 공간 낭비 가능성
- 재고 회전율 저하

**예시:**
- 안전재고: 20개
- 현재 재고: 85개
- 판정: **과다** (85 > 20 × 3 = 60)

**대응 방법:**
1. 프로모션 진행 고려
2. 향후 발주량 조정
3. 창고 공간 재배치
4. 재고 회전율 개선 전략 수립

---

## 🔄 상태 변경 시점

재고 상태는 다음 이벤트 발생 시 자동으로 재계산됩니다:

### 1. 입고(IN)
```javascript
재고 수량 증가 → 상태 재평가
예: 부족(15개) + 입고(10개) = 정상(25개)
```

### 2. 출고(OUT)
```javascript
재고 수량 감소 → 상태 재평가
예: 정상(25개) - 출고(20개) = 부족(5개)
```

### 3. 재고 조정(ADJUST)
```javascript
재고 수량 수정 → 상태 재평가
예: 실사 결과 정상(50개) → 조정 → 부족(12개)
```

### 4. 안전재고 변경
```javascript
안전재고 기준 변경 → 모든 상태 재평가
예: 안전재고 20개 → 30개로 변경
     현재 25개: 정상 → 부족
```

---

## 💻 코드 구현 위치

### 프론트엔드
```
frontend/js/inventory.js
- getStockStatusBadge(qty, safetyStock)
  → 상태 판정 및 배지 HTML 생성

frontend/js/products.js
- 상품 목록 렌더링 시 상태 표시

frontend/js/dashboard.js
- 대시보드 재고 현황에 상태 표시
```

### 백엔드
```
backend/controllers/productController.js
- 상태 판정 로직 없음 (프론트엔드에서 처리)
- 재고 수량과 안전재고만 저장
```

---

## 📊 상태별 대시보드 통계

대시보드에서는 다음 통계를 제공합니다:

```
✅ 전체 상품 수: 모든 상품
⚠️ 재고 부족: 부족 + 긴급 + 품절 상품 수
📈 재고 과다: 과다 상품 수
```

---

## 🎨 UI 표시 방식

### 배지 스타일
```css
.badge-danger  { 품절, 긴급 - 빨강 배경 }
.badge-warning { 부족 - 노랑 배경 }
.badge-success { 정상 - 초록 배경 }
.badge-info    { 과다 - 파랑 배경 }
```

### 표시 위치
- 상품 관리 탭: 상태 열
- 재고 관리 탭: 상태 열
- 대시보드: 재고 현황 테이블
- 바코드 탭: 조회 결과

---

## 📝 개발자 참고사항

### 상태 판정 로직
```javascript
function getStockStatusBadge(qty, safetyStock) {
  if (qty === 0) 
    return '<span class="badge badge-danger">품절</span>';
  
  if (qty < safetyStock * 0.5) 
    return '<span class="badge badge-danger">긴급</span>';
  
  if (qty < safetyStock) 
    return '<span class="badge badge-warning">부족</span>';
  
  if (qty > safetyStock * 3) 
    return '<span class="badge badge-info">과다</span>';
  
  return '<span class="badge badge-success">정상</span>';
}
```

### 주의사항
1. `safetyStock = 0`인 경우 처리 필요
2. 소수점 수량(단위 m) 고려
3. 실시간 상태 업데이트 필수

---

## 🔍 사용자 가이드

### 상태 확인 방법
1. 대시보드 → 재고 현황 표에서 한눈에 확인
2. 재고 관리 탭 → 상태 열에서 개별 확인
3. 상품 관리 탭 → 상태 열에서 확인

### 필터링
- 대시보드 카드 클릭 시 해당 상태만 필터링
- 예: "재고 부족" 카드 클릭 → 부족/긴급 상품만 표시

---

**작성일:** 2026-02-08  
**버전:** 1.0  
**작성자:** ERP 시스템 개발팀
